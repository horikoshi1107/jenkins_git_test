<?xml version="1.0" encoding="utf-8" ?>
<project name= "BuildTest" basedir= "." default= "main">

  <target name="main" depends= "init,phpcs,phpmd,phpcpd"></target>

  <!-- init -->
  <target name="init">
    <delete dir= "./reports" includeemptydirs= "true" />
    <mkdir dir= "./reports" />
  </target>

  <!-- phpcs -->
  <target name="phpcs">
    <exec executable= "phpcs" output= "reports/phpcs.xml">
      <arg line= "--report=checkstyle /tmp" />
    </exec>
  </target>

  <!-- phpmd -->
  <target name="phpmd">
    <phpmd rulesets= "cleancode,codesize,controversial,design,naming,unusedcode">
      <fileset dir= "/var/www/jenkins_git_test">
        <include name= "**/*.php" />
        <exclude name= "**/*Test.php" />
      </fileset>
      <formatter type= "xml" outfile= "phpmd.xml" />
    </phpmd>
  </target>

  <!-- phpcpd -->

  <!-- phpcpd
  <target name="phpcpd">
    <phpcpd>
      <fileset dir= "/var/www/jenkins_git_test">
        <include name= "**/*.php" />
        <exclude name= "**/*Test.php" />
      </fileset>
      <formatter type= "pmd" outfile= "phpcpd.xml" />
    </phpcpd>
  </target>
-->
</project>
